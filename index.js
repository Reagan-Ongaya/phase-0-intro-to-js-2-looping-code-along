for (let age = 30; age < 40; age++) {
    console.log(`I'm ${age} years old. Happy birthday to me!`);
    debugger;
}

const gifts = ["teddy bear", "drone", "doll"];

function wrapGifts(gifts) {
    for (let i = 0; i < 3; i++) {
        console.log(`Wrapped ${gifts[i]} and added a bow!`);
        debugger;
    }

    return gifts;
}

wrapGifts(gifts);

writeCards(["Guadalupe", "Ollie", "Aki"], "birthday");

const child = ["Guadalupe", "Ollie", "Aki"];

function writeCards(child){
    for(let i = 0; i < 3; i++){
        console.log('Thank you  ${child[i]}for the wonderful birthday gift!');
        debugger;
    }

    return child;
}

writeCards();

function countDown(number){
    console.log(whiteCard[child]);
}

countDown(number);



function countDown(numbers){
for (let age = num1; num < 1; ++1) {
    console.log(num);
    debugger;
}}
